import styled from 'styled-components';
import { flex } from '../../../styles/variables';

const StyledChocolates = styled.div`
  ${flex()};
  
  flex-wrap: wrap;
`;

export default StyledChocolates;
